# Frontend Environment Variables
# Copy to .env and set values

# Backend API base URL (used when NOT using the Vite proxy)
VITE_API_BASE_URL=http://localhost:8000/api/v1

# App display name
VITE_APP_NAME="NGO Transparency Platform"
