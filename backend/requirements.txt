# ─────────────────────────────────────────────────────────────────
# NGO Transparency Platform — Backend Dependencies
#
# VERSION PINS: Using >= ranges (not ==) so pip can resolve
# pre-built binary wheels for any Python version (3.12 / 3.13 / 3.14).
# Pin to exact versions in production via `pip freeze > requirements.lock`.
# ─────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.29.0                 # async PostgreSQL driver
geoalchemy2>=0.15.0             # PostGIS geometry support
alembic>=1.13.0                 # database migrations
psycopg[binary]>=3.2.10        # sync driver for Alembic (pre-built Windows wheels)

# Configuration
pydantic-settings>=2.5.0
pydantic[email]>=2.9.0

# Authentication / Security
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0              # direct bcrypt usage; passlib is unmaintained and incompatible with bcrypt 5.x
python-multipart>=0.0.12

# HTTP / utilities
httpx>=0.27.0
python-dateutil>=2.9.0

# Logging
structlog>=24.4.0

# Testing (dev)
pytest>=8.3.0
pytest-asyncio>=0.24.0
aiosqlite>=0.20.0
# httpx is already listed above and also used as an async test client

